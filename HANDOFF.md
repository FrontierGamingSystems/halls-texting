# Handoff for the AndrewRinglein repository

Work in https://github.com/AndrewRinglein/halls-texting and connect it to the existing Supabase project gdvzebqxecmanobdbhha (Bingo Halls and Texting): https://gdvzebqxecmanobdbhha.supabase.co.

1. Inspect the repository before editing. The last audit found a flattened upload: 118 files lost their directories and supabase/README.md was missing. Restore the folder structure from https://github.com/FrontierGamingSystems/halls-texting, preserving any intentional newer changes. Keep the normal app/, lib/, components/, scripts/, tests/, supabase/, .github/ and .openai/ paths and dotfiles. Do not force-push over user changes.
2. Reuse the existing Supabase tables and messages. Do not create another project or reset the database. Tables are bingo_halls, bingo_messages, bingo_message_halls, bingo_state and bingo_link_mappings. Read the existing migrations before applying anything; the original migration was applied through SQL Editor, so the migration ledger alone does not establish whether its schema exists.
3. Configure server-only TD_API_KEY, TD_NUMBER, BINGO_SYNC_KEY, SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in the deployment environment. Obtain existing secret values through the owner's secure environment, not source files or chat. The monitored number is 12793469150. Never commit secrets or expose them in browser code.
4. This is a Sites/vinext application for Cloudflare Workers, not a static GitHub Pages site. The current live URL is https://frontier-bingo-text-monitor.andrew595321.chatgpt.site/ and the existing Sites project is appgprj_6aa3c8174f608191ae8d292ebc795086. Establish whether changes should publish there or to a new host before deployment. A GitHub push currently runs validation only. A different host needs compatible Worker deployment configuration and replacement for Sites-specific owner authentication.
5. Supabase now has ONE active cron job named bingo-sms-sync, every five minutes, calling the current live app's POST /api/monitor. Its x-bingo-sync-key header reads Vault secret bingo_monitor_sync_key. Preserve this job and the existing secret. If moving hosting, verify the replacement endpoint first, then update this same job's URL. Do not create duplicate collectors. See supabase/schedule-sync.sql.
6. The application reseeds halls when the bundled directory hash changes. Avoid running conflicting old/new deployments against the same database; preserve current hall metadata and attribution rules, and use one authoritative deployed version.
7. Keep cards sorted by latest promotional text, with all-date message counts and full history in View details. Only procedural messages should be hidden. Shared shortcodes require hall-specific attribution. Public viewers can read, but must not gain write access or private sync-state access.
8. Use Node 22.13+; run npm run install:ci, npm test, npx tsc --noEmit, and npm run build. Test actual authenticated sync, deduplication, multi-message history, public read-only permissions and automatic collection with the browser closed. Check HTTP responses and advancing last_sync, not merely a successful cron SQL execution.

Remaining work includes failure alerts, eight previously unresolved hall attributions, pending web signups and more statewide signup research. The last confirmed enrollment count was 19; query the database for current figures. The cron job is configured, but a separate 15-minute failure notification has not been configured.
